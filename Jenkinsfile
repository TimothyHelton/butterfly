pipeline {
  agent {
    docker {
      image 'butterfly_python:latest'
    }
  }
  stages {
    stage('test') {
      steps {
        sh '# TODO Update code here -> python --version'
      }
    }
  }
}
